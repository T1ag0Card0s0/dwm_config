# Setup

To install everything

```bash
sudo make USER_HOME=$HOME
```

To install each feature individually

```bash
sudo make <dmenu|dwm|slock|slstatus|st> USER_HOME=$HOME
```

To share neovim clipboard with dwm

```bash
sudo pacman -S xclip
```
